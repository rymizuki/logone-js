{
  "name": "logone",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@logone/dev-config": "*",
    "turbo": "^1.13.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
