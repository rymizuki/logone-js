{
  "name": "@logone/adapter-node",
  "private": false,
  "version": "0.0.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsup --dts --outDir build --splitting false --sourcemap --clean ./src/index.ts",
    "test": "vitest run",
    "lint": "eslint --max-warnings 0 --ext .ts,.tsx .",
    "format": "prettier -w './**/*.{ts,tsx,js,json,css}' --check"
  },
  "devDependencies": {
    "@logone/core": "*",
    "@logone/dev-config": "*",
    "@logone/test-helper": "*",
    "@types/node": "^20.10.4",
    "tsup": "^8.0.2",
    "vitest": "^1.0.4"
  }
}
